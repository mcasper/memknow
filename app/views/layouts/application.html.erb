<!DOCTYPE html>
<html>
  <head>
    <title>Memknow</title>
    <%= stylesheet_link_tag "application", "main", media: "all", "data-turbolinks-track" => true %>
    <%= stylesheet_link_tag "application", "http://fonts.googleapis.com/css?family=Open+Sans:400,700,600" %>
    <%= favicon_link_tag 'favicon.ico' %>
    <%= javascript_include_tag "application", "sidebar", "data-turbolinks-track" => true %>
    <%= csrf_meta_tags %>
  </head>
  <body>
    <div class="header">
    <% if signed_in? %>
      Logged in as <%= @current_user.email %> | <%= link_to "Sign out", signout_path, :method=>:delete %>
    <% else %>
      <%= link_to "Sign in", signin_path %>
    <% end %>
    </div>


    <div class="container wrap">
      <%= render 'shared/banner' %>

      <br/>

      <%= yield %>

      <% if !signed_in? %>
      <%= render 'shared/infographic' %>
      <% end %>
    </div> <!-- container -->

    <div id="push"></div>
    <footer>
      <div class="container">
        <p class="muted credit" id="footer">
          &copy;2014. Development version of Memknow Intelligent Flashcard software created by Eric Kopp.
        </p>
      </div>
    </footer>
  </body>
</html>

